spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ticketing
    username: ticketing
    password: ticketing
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false