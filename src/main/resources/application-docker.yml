spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ticketing
    username: ticketing
    password: ticketing
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
